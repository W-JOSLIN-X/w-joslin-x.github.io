# 本地验收记录

日期：2026-09-22。预览地址：http://127.0.0.1:4321/ 。未发布到 GitHub。

- Astro 静态构建成功，生成首页、关于页、404 与 6 篇演示文章。
- Astro 类型检查：0 errors；之后移除两处未使用的上游字体引用。
- 搜索单元测试 3 项通过：标题/正文范围差异、主题标签 AND 与情绪 OR 的组合、全半角及大小写处理。
- 浏览器验证：标题搜索“梯度”无结果，全文模式返回公式示例；Markdown + 写作标签返回两篇，同时选择平静/思考保留两篇，追加数学标签返回空状态。
- 浏览器验证：公式与 Mermaid 正常渲染；代码复制保留缩进与空行，不包含行号。
- 浏览器验证：脚注、更新时间、反向引用和标签文章切换的数据存在。
- 桌面与 390×844 手机尺寸已检查；手机筛选和个人资料抽屉可展开关闭；默认页面无横向溢出。
- 外观设置的横幅/全屏、深浅主题已实际切换；交付前恢复浅色横幅默认值。
- ZIP 可正常读取，包含 Markdown 与本地图片，相对图片路径已验证；无图文章生成 Markdown。
- 输出文件静态检查：10 个 HTML 文件的 189 个内部链接/资源存在（含主题公共资源页面），无归档路由；默认页面无损坏图片。
- 活跃图本地为空，未伪造提交历史；正式仓库完整历史下的真实记录待发布阶段验证。
- 不蒜子仅在正式域名加载，本地未显示计数；正式域名统计待发布后验证。
- 音乐无真实音源，显示明确占位并禁用播放；网页宠物未启用。

尚未完成的外部步骤：登录 GitHub、将源码合入用户仓库、配置部署工作流、发布并验证正式地址与统计。用户本轮只要求本地搭建。
