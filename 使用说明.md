# Roooooxy’s BLOG 使用说明

本地网站基于 Mizuki 9.0 / Astro 定制。发布仓库为 W-JOSLIN-X/w-joslin-x.github.io，main 分支更新后由 GitHub Actions 自动构建并发布。

## 打开与更新预览

在本目录打开终端：

```powershell
pnpm install
pnpm build
pnpm preview --host 127.0.0.1 --port 4321
```

浏览器打开 http://127.0.0.1:4321/ 。预览命令运行期间保持终端开启。若修改文章后需要更新静态预览，再运行 `pnpm build`。

边修改边预览可运行 `pnpm dev`（默认端口 3000）；新增或更换图片后需要重新运行 `node scripts/prepare-roxy.mjs` 以刷新下载包。正式构建会自动执行。

## 写一篇文章

在 `src/content/posts/` 下创建文章目录，例如 `my-first-note`，并创建 `index.md`：

```markdown
---
title: 我的第一篇笔记
published: 2026-09-22
updated: 2026-09-22
description: 用一两句话概括这篇文章。
category: 学习笔记
tags: [编程, 学习]
moods: ["🤔 思考"]
---

## 从一个问题开始

这里写正文。
```

- `title`、`published` 必填。一个 `category` 表示类型；多个 `tags` 表示主题。
- `moods` 可省略或使用空数组，情绪不必建立严格分类。
- 修改文章时填写 `updated`；首页顺序仍取 `published`，不会因为更新而移到最上面。
- 可选 `image: /images/roxy/makeine-03.jpg` 设置卡片封面，不填写时没有封面空位。
- 可选 `background: /images/roxy/makeine-04.jpg` 为这篇设置背景；`backgroundPosition: "50% 35%"` 调整图片焦点。未配置时继承首页背景。
- 可选 `sourceLink: https://...` 添加原文出处。
- 发布地址由目录名决定：`my-first-note/index.md` 对应 `/posts/my-first-note/`。文章互链使用 `[标题](/posts/my-first-note/)`，自动反向引用会识别这种标准链接。
- 现有六篇均为带“演示”标签的示例内容，可以删除或替换；不要重新运行 `scripts/setup-roxy.mjs`，它仅用于首次初始化演示素材。

## 本地图片与下载

```text
src/content/posts/my-first-note/
  index.md
  images/
    illustration.jpg
```

在文章里写 `![图片说明](./images/illustration.jpg)`。构建时自动收集正文用到的本地图片并生成 ZIP；包内 Markdown 路径会调整为可离线阅读的相对路径。没有本地图片的文章生成 `.md` 文件。外链图片保持外链，不自动抓取。

目前打包识别标准 Markdown 行内图片语法；请使用上述写法，避免 HTML 图片或引用式图片写法。图片与文件名尽量不含空格。ZIP 使用不压缩存储模式以保持稳定，包含完整原图。

生成文件在 `public/downloads/`。文章有本地直接下载、GitHub 源文和仓库文件入口；网站下载在每次部署时自动更新。仓库内的下载包需本地构建后连同修改一起提交，ZIP 已设置为可提交文件。

其他附件在文章元信息里按需配置：

```yaml
attachments:
  - name: 课程资料 PDF
    url: https://github.com/W-JOSLIN-X/w-joslin-x.github.io/blob/main/资料路径.pdf
```

## 修改外观和个人资料

编辑 `src/config/roxy.ts`：

- `title`、`name`、`subtitle`、`motto`：网站名称、昵称、副标题、格言。
- `avatar`：头像路径，当前使用站主提供的图片。
- `background` 和 `backgroundPosition`：默认背景及焦点。
- `announcement`：公告正文。空字符串时隐藏公告卡片。
- `music.src`：音乐地址或 `/music/example.mp3`；填入后播放器可用，默认不自动播放。`music.title` 和 `music.artist` 修改显示信息。
- `statistics`：是否接入访问统计。

顶部调色盘可切换背景模式、主题色、柔化、波浪和标题动态；偏好只保存在当前浏览器。月亮按钮切换深浅模式。

## 活跃图与访问量

活跃图读取 `src/content/posts/` 的真实 Git 提交，每个提交计一次，使用提交时间按北京时间分日。图片等文章相关资源放进相应文章目录即可纳入统计。只修改配置或生成文件不会计入。

发布工作流使用完整 Git 历史（`fetch-depth: 0`），在构建时自动生成本站活动记录。

不蒜子仅在 `w-joslin-x.github.io` 正式域名加载。本地预览隐藏计数，避免显示服务给 localhost 返回的共享计数。将来启用自定义域名时，需要同步修改 `src/layouts/RoxyLayout.astro` 中的域名判断以及 `src/config/siteConfig.ts` 中的站点地址。统计服务失败时不显示数字，正文和搜索仍可使用。

## 自动发布

将修改提交到 main 分支，GitHub Actions 会检查搜索逻辑、构建页面并验证内部链接，然后部署到 https://w-joslin-x.github.io/ 。首次版本标记为 v1.0.0。部署进度见仓库 Actions 页面。

## 图片出处

`public/images/roxy/makeine-03.jpg`、`makeine-04.jpg`、`makeine-06.jpg` 来自 https://makeine-anime.com/ 的公开宣传视觉图，完整地址见 `docs/素材来源.md`。它们是背景与封面占位，非本项目原创或开放许可素材。发布用图可统一替换。

## 本地检查

```powershell
pnpm build
pnpm test:roxy
```

可直接查看“把公式、代码与流程放进同一篇笔记”验证公式、代码和 Mermaid；“图片与附件的整理方式”验证本地图片和 ZIP；“Markdown 写作清单”验证脚注和反向引用。
